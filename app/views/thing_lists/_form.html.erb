<%= form_for(@thing_list) do |f| %>
  <% if @thing_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thing_list.errors.count, "error") %> prohibited this thing_list from being saved:</h2>

      <ul>
      <% @thing_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
